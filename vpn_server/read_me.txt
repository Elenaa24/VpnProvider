How to install a server?
    1. Copy the vpn_server folder to your linux system.
    2. Run (with root privilage) the install_server.py script. The system will be restarted.
    3. Wait to power on
    4. If the http server is running the installation is done, otherwise check the logs. 